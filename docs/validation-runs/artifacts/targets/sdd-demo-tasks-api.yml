# Target registry entry — created via /dev.target register (greenfield --new).
id: sdd-demo-tasks-api
path: "X:/Work/sdd-demo-tasks-api"
vcs: git
default_branch: main
stack: [node, typescript]
test_command: "node --test"            # verified during scaffold
lint_command: "npx tsc --noEmit"       # typecheck as the lint gate; verified during scaffold
build_command: "npx tsc -p ."
complexity_class: MEDIUM
conventions: |
  Greenfield. Zero-runtime-dependency by choice: TypeScript + Node built-in test runner
  (node:test), in-memory store, handlers behind a reader port so they unit-test without a
  live socket. Conventions emerge from the scaffold ADRs.
exceptions: []
status: active
registered: 2026-06-11
