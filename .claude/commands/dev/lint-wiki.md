---
description: "Wiki health check including dual-runtime skill parity"
argument-hint: "[--write]"
---

## Arguments

$ARGUMENTS

Adopt the **archivist** subagent persona (`.claude/agents/archivist.md`) — delegate to it via the Agent tool when running as a top-level conversation.

Follow the canonical runbook at `.specify/extensions/dev/commands/dev.lint-wiki.md` step-by-step: preconditions, steps, exit criteria, failure modes. The constitution at `.specify/memory/constitution.md` overrides everything else.

Bootstrap first (Constitution Principle II); append the outcome to `wiki/log.md` (Principle VII).
